{
  "name": "kevith123-main",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "python -m http.server 8000",
    "dev": "concurrently \"npx tailwindcss-cli -i ./input.css -o ./style.css --watch\" \"npm start\"",
    "build": "npx tailwindcss-cli -i ./input.css -o ./style.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "tailwindcss": "^4.1.18",
    "tailwindcss-cli": "^0.1.2"
  }
}
